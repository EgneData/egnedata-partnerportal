spring:
  datasource:
    url: jdbc:tc:postgresql:16:///partnerportal
    username: test
    password: test
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

partnerportal:
  web-origin: https://web.example.com
